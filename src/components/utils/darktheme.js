import React from 'react';
import PropTypes from 'prop-types';

const Darktheme = props => {
    return (
        <div>
            <themeButton>Oui</themeButton>
        </div>
    );
};

const themeButton = styled.button`

`

export default Darktheme;